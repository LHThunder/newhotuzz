# Specs

Mỗi tính năng một thư mục `NNN-slug/` gồm `spec.md` (WHAT & WHY), `plan.md` (HOW), `tasks.md` (việc cụ thể).

Quy trình: `/specify` → `/clarify` → `/plan` → `/tasks` → `/analyze` → `/implement`.
Xem toàn cảnh: `/spec-status`. Nguyên tắc dự án: `.specify/memory/constitution.md`.
